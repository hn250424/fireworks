enum EXPLOSION {
    BURST,
    ERUPT,
    BLOOM,
    CHAIN_BURST
}

enum EVENT {
    ONE,
    TWO_OR_THREE,
    FIVE
}

const TYPE = {
    EXPLOSION,
    EVENT,
}

export default TYPE